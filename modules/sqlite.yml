name: sqlite
sources:
  - type: git
    url: https://github.com/sqlite/sqlite.git
    tag: version-3.53.2
    commit: ccc132c5be20ab5c755c97a08d06b1b592fef330
    x-checker-data:
      type: git
      tag-pattern: version-([\d.]+)$
buildsystem: simple
builddir: true
build-commands:
  - CFLAGS="-DSQLITE_ENABLE_COLUMN_METADATA=1" ./configure --prefix=/app --disable-static
    --enable-all #enable-all is only; fts4/5 geopoly rtree session
  - make
  - make install
