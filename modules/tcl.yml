name: tcl
sources:
  - type: git
    url: https://github.com/tcltk/tcl.git
    tag: core-9-0-1
    commit: 6230a12b3bf6f7d8128f6ef14b0a3650a9d479e0
    x-checker-data:
      type: git
      tag-pattern: core-(\d+-\d+-\d+)$
buildsystem: simple
builddir: true
subdir: unix
build-commands:
  - ./configure --prefix=/app --enable-64bit
  - make
  - make install
post-install:
  - chmod 755 /app/lib/libtcl*.so
